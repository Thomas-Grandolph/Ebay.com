export class ButtonModel {

    img: string;
    text: string;
    color: string;
    textColor: string;

    constructor(img: string, text: string, color: string, textColor:string) {
        this.img = img;
        this.text = text;
        this.color = color;
        this.textColor = textColor;

    }
}